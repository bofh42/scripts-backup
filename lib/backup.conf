# SPDX-License-Identifier: GPL-2.0-or-later
# Copyright (C) 2023-present Peter Tuschy (foss@bofh42.de)

# this is the used by backup.sh (not sourced)

# destination host(s) for backup
# use fqdn and not IP (for now)
# like CFG_DEST_1=borg.yourdomain.de
# or use --server on the command line
# or -[2-9] and defined CFG_DEST_[2-9]
# for now CFG_DEST_[1-9], BORG_REPO_[1-9], CFG_CREATE_[1-9] is supported
# and     CFG_PRUNE_HOURLY, CFG_PRUNE_DAILY, CFG_PRUNE_WEEKLY, CFG_PRUNE_MONTHLY, CFG_PRUNE_YEARLY

CFG_DEST_1=

# optional define different parameter then the script does by defautl, but you need to give -1 on the command line
#CFG_CREATE_1=" --error --exclude-if-present .nobackup --keep-exclude-tags --exclude-caches --exclude-nodump $(ver_ge $CFG_VER $CFG_VER12 || echo "--noatime")"
#CFG_PRUNE_HOURLY_1=13
#CFG_PRUNE_DAILY_1=8
#CFG_PRUNE_WEEKLY_1=6
#CFG_PRUNE_MONTHLY_1=18
#CFG_PRUNE_YEARLY_1=3
# you can set every borg supported repo
# ssh is default and looks like this
#BORG_REPO_1=ssh://zborg@${CFG_DEST_1%%.*}/~/hosts/${HOSTNAME%%.*}


# 1 is default for CFG_DEST
# for 2-9 you need a extra call of borgbackup-server.sh -[2-9]
CFG_DEST_2=
CFG_DEST_3=
CFG_DEST_4=
CFG_DEST_5=
CFG_DEST_6=
CFG_DEST_7=
CFG_DEST_8=
CFG_DEST_9=
